[package]
name = "headrace-proto-gen"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Codegen for Headrace's checked-in gRPC stubs; a dev tool, not published."
publish = false

[dependencies]
tonic-prost-build.workspace = true
